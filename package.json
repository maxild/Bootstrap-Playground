{
  "name": "bootstrap-playground",
  "version": "1.0.0",
  "description": "Playing with bootstrap v4.1.3 with Sass\"",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "npx sass ./styles.scss build/styles.css",
    "build": "npx postcss *.css --use autoprefixer -d build/"
  },
  "author": "Morten Maxild",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^4.1.3"
  },
  "devDependencies": {
    "autoprefixer": "^9.4.2",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.6",
    "postcss-cli": "^6.0.1"
  }
}
